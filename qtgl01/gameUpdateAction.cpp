#include "gameupdateaction.h"
#include<QGLWidget>

/*
void gameUpdateAction::renderingMaps(GameState& gstate){

    glBegin(GL_LINES);                      // es
        glColor3f(0.0f,1.0f,1.0f);
        glVertex3f( 0.0f, 1.0f, 0.0f);
        glVertex3f( 0.0f, 0.0f, 0.0f);
        glVertex3f( 0.0f, 3.0f, 0.0f);
        glVertex3f( 4.0f, -5.0f, 0.0f);
    glEnd();

}
void gameUpdateAction::renderingPlayers(GameState& gstate){
    glBegin(GL_TRIANGLES);                      // Drawing Using Triangles
        glColor3f(1.0f,0.0f,1.0f);
        glVertex3f( 0.0f, 2.0f, 0.0f);              // Top
        glVertex3f( -1.0f, 0.0f, 0.0f);              // Bottom Left
        glVertex3f( 1.0f, 0.0f, 0.0f);              // Bottom Right
    glEnd();
}
void gameUpdateAction::renderingEnemies(GameState& gstate){
    glBegin(GL_POLYGON);
        glColor3f(0.0f,0.0f,1.0f);
        glVertex3f( 1.0f, -1.0f,1.0f);
        glVertex3f(-1.0f, -1.0f,1.0f);
        glVertex3f(-1.0f, 0.0f,1.0f);
        glVertex3f( 1.0f, 0.0f,1.0f);
    glEnd();
}
*/
